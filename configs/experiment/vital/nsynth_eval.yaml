# @package _global_

defaults:
  - vital/base_full
  - _self_

# Experiment metadata
experiment_name: vital
run_name: nsynth_eval

# Data configuration
data:
  dataset_root: ./datasets/nsynth_eval
  batch_size: 64
  num_workers: 8

model:
  num_params: 597

# Trainer configuration
# (Evaluation doesn't technically train, but leaving standard parameters)
trainer:
  min_epochs: 1
  max_epochs: 400
