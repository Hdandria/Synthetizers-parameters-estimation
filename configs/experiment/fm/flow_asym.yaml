# @package _global_

defaults:
  - fm/base
  - override /model: ksin_flow_xform

run_name: flow_xform_asym

model:
  vector_field:
    num_tokens: ${out_dim}
    projection:
      num_tokens: ${out_dim}
      num_params: ${out_dim}

data:
  break_symmetry: true
