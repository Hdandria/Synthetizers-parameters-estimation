# @package _global_
_self_: true

defaults:
  - override /experiment: flow_multi/base_full

# Experiment metadata
experiment_name: flow_multi
run_name: projection_penalty_low

# Model configuration
model:
  vector_field:
    projection_penalty: 0.001
