# @package _global_

defaults:
  - override flow_multi/base_full

# Experiment metadata
experiment_name: flow_multi
run_name: model_enlarged

# Model configuration
model:
  encoder:
    d_model: 768
    n_heads: 12
    n_layers: 12
  vector_field:
    d_model: 768
    num_heads: 12
    num_layers: 12
