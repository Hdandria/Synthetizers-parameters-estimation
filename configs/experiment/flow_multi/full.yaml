# @package _global_
_self_: true

defaults:
  - override /experiment: flow_multi/base_full

# Experiment metadata
experiment_name: flow_multi
run_name: full

# Trainer configuration
trainer:
  precision: 16-mixed

  
