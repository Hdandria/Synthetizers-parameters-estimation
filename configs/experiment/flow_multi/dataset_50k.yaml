# @package _global_

defaults:
  - override flow_multi/base_full

# Experiment metadata
experiment_name: flow_multi
run_name: dataset_50k

# Data configuration
data:
  dataset_root: ./datasets/surge-50k

# Trainer configuration
trainer:
  min_steps: 50000
  max_steps: 50000
