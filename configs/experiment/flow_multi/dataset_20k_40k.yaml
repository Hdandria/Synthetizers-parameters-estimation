# @package _global_

defaults:
  - flow_multi/base_full
  - _self_

# Experiment metadata
experiment_name: flow_multi
run_name: dataset_20k_40k

# Data configuration
data:
  dataset_root: ./datasets/surge-20k

# Trainer configuration
trainer:
  min_steps: 40000
  max_steps: 40000
