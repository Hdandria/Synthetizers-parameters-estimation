# @package _global_
_self_: true

defaults:
  - override /experiment: flow_multi/base_full

# Experiment metadata
experiment_name: flow_multi
run_name: batch_size_high

# Data configuration
data:
  batch_size: 256