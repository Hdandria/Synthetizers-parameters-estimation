# @package _global_

defaults:
  - override flow_multi/base_full

# Experiment metadata
experiment_name: flow_multi
run_name: dataset_50k_200k

# Data configuration
data:
  dataset_root: s3://${oc.env:S3_BUCKET}/datasets/surge-50k

# Trainer configuration
trainer:
  min_steps: 200000
  max_steps: 200000
