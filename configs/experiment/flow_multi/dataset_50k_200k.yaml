# @package _global_

defaults:
  - flow_multi/base_full
  - _self_

# Experiment metadata
experiment_name: flow_multi
run_name: dataset_50k_200k

# Data configuration
data:
  dataset_root: ./datasets/surge-50k

# Trainer configuration
trainer:
  min_steps: 200000
  max_steps: 200000
