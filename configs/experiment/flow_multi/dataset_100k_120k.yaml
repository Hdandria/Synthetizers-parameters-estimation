# @package _global_

defaults:
  - flow_multi/base_full
  - _self_

# Experiment metadata
experiment_name: flow_multi
run_name: dataset_100k_120k

# Data configuration
data:
  dataset_root: ./datasets/surge-100k

# Trainer configuration
trainer:
  min_steps: 1200000
  max_steps: 1200000
