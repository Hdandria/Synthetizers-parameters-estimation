# @package _global_

defaults:
  - flow_multi/base_full
  - _self_

# Experiment metadata
experiment_name: flow_multi
run_name: dataset_20k_20k

# Data configuration
data:
  dataset_root: ./datasets/surge-20k

# Trainer configuration
trainer:
  min_steps: 20000  
  max_steps: 20000
  val_check_interval: 1000